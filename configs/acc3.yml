
- name: "ethernet-1/3"
  admin-state: enable
  vlan-tagging: false
  subinterface:
    - index: 0
      type: bridged
      admin-state: enable
      vlan:
       encap:
        single-tagged:
         vlan-id: 20

- name: "ethernet-1/4"
  admin-state: enable
  vlan-tagging: false
  subinterface:
    - index: 0
      type: bridged
      admin-state: enable
      vlan:
       encap:
        single-tagged:
         vlan-id: 30

- name: "ethernet-1/1"
  admin-state: enable
  vlan-tagging: true
  subinterface:
    - index: 0
      type: bridged
      admin-state: enable
      vlan:
        encap:
          single-tagged:
            vlan-id: 10
    - index: 1
      type: bridged
      admin-state: enable
      vlan:
       encap:
        single-tagged:
         vlan-id: 20
    - index: 2
      type: bridged
      admin-state: enable
      vlan:
       encap:
        single-tagged:
         vlan-id: 30

- name: "ethernet-1/2"
  admin-state: enable
  vlan-tagging: true
  subinterface:
    - index: 0
      type: bridged
      admin-state: enable
      vlan:
        encap:
          single-tagged:
            vlan-id: 10
    - index: 1
      type: bridged
      admin-state: enable
      vlan:
       encap:
        single-tagged:
         vlan-id: 20
    - index: 2
      type: bridged
      admin-state: enable
      vlan:
       encap:
        single-tagged:
         vlan-id: 30
